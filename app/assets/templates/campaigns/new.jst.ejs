<div class="panel-title host-header">
  <h1>Be a Dungeon Master</h1>
  <p>Don't be like this guy. Make some friends.</p>
  <br>
  <button class="btn btn-success btn-lg" data-toggle="modal" data-target="#newCampaignModal">
    Tell Us About Your Campaign
  </button>
</div>

<div class="modal fade" id="newCampaignModal" tabindex="-1" role="dialog" aria-labelledby="newCampaignModal" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title" id="newCampaignModal">Basic Info</h4>
      </div>
      <div class="modal-body">
        <form>
          <label>Name Your Campaign
            <input type="text" name="campaign[title]" class="form-control">
          </label><br>
          <label>When is it?<br>
            <div class='input-group'>
              <input class="span2 datepicker form-control" id="start_date" size="16" type="text" data-date-format="dd-mm-yyyy" name="campaign[start_date]" placeholder="Choose a start date"><span class="input-group-addon"><span class="glyphicon glyphicon-calendar"></span></span>
            </div>
            <div class='input-group'>
              <input class="span2 datepicker form-control" id="end_date" size="16" type="text" data-date-format="dd-mm-yyyy" name="campaign[end_date]" placeholder="Choose an end date"><span class="input-group-addon"><span class="glyphicon glyphicon-calendar"></span></span>
            </div>
          </label><br>
          <label>Location
            <input type="text" name="campaign[location]" class="controls form-control" id="pac-input" placeholder="Where do you want to play?">
          </label><br>
          <label>Game Style
            <select name="campaign[game_style]" class="form-control">
              <% _.each(gameStyles, function(gameStyle) { %>
                <option value="<%= gameStyle %>"><%= gameStyle %></option>
              <% })%>
            </select>
          </label><br>
          <label>Game System
            <select name="campaign[game_system]" class="form-control">
              <% _.each(gameSystems, function(gameSystem) { %>
                <option value="<%= gameSystem %>"><%= gameSystem %></option>
              <% })%>
            </select>
          </label><br>
          <label>Campaign Setting
            <select name="campaign[setting]" class="form-control">
              <% _.each(settings, function(setting) { %>
                <option value="<%= setting %>"><%= setting %></option>
              <% })%>
            </select>
          </label><br>
          <label>Number of Players
              <input type="number" name="campaign[num_members]" class="form-control" min="1">
            </label><br>
          </label><br>
          <input type="submit" value="Continue" class="btn btn-success">
        </form>
      </div>
    </div>
  </div>
</div>
